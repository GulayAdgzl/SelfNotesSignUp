import 'package:flutter/material.dart';

class ProjectPadding {
  static const pagehorizontal = EdgeInsets.symmetric(horizontal: 16);
  static const pagebottom = EdgeInsets.only(bottom: 40);
  static const pageright = EdgeInsets.only(right: 16);
  static const pagebottom3 = EdgeInsets.only(bottom: 30);
}
